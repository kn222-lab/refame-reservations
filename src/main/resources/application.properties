server.port=${PORT:8080}

spring.application.name=reframe

# データベース接続設定（Renderの環境変数から取得、未設定時は現状のRDS）
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db-reframe-dev.c38au42gu20l.ap-northeast-1.rds.amazonaws.com:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:a2hW2X87}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate 設定
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# メール送信設定 (Gmail SMTP - 465 SSL)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:reframe.namiki@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rqhztusklfpqyioi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory

# 管理者通知先のメールアドレス
app.admin.email=${ADMIN_EMAIL:k.n.bays.222@gmail.com}

# アプリケーションのベースURL（デフォルトはローカル環境用）
app.base-url=https://refame-reservations.onrender.com